/* data · diagnostics/tranche-c — Wave 5 PR-6c content tranche.

   ~24 syndromes: bloodstream (second half) + genitourinary +
   intra-abdominal. Authored at the apex bar per the schema in the
   aggregator. Authored in parallel with tranche-b/d/e per the
   plan's multi-agent matrix.

   Schema documented in src/data/diagnostics.js (the aggregator).
   Inpatient Antibiotic Guide — module graph in README.md. */

export const TRANCHE_C_DIAGNOSTICS = {

};
